server.port=8082
spring.application.name=ACTOR-SERVICE
spring.datasource.url=jdbc:mysql://mysqldb:3306/priam-actor?useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.database=MYSQL
spring.cloud.discovery.enabled=true

eureka.instance.prefer-ip-address=true
eureka.client.service-url.defaultZone=${EUREKA_URL:http://eureka:8761}/eureka/
# Enable self-registration
eureka.client.register-with-eureka=true
# Enable fetching registry information
eureka.client.fetch-registry=true

feign.client.config.default.connectTimeout=160000000
feign.client.config.default.readTimeout=160000000

# keycloak.realm=priam-realm
# keycloak.resource=Data-client
# keycloak.bearer-only=true
# keycloak.auth-server-url=${KEYCLOAK_AUTH_SERVER_URL:http://localhost:8080}
# keycloak.ssl-required=none
# keycloak.cors=true
#Prometheus
management.endpoints.web.exposure.include=prometheus
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
# spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_SERVER_URL:http://localhost:8080}/realms/priam-realm
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs
