<!DOCTYPE html>

<html>
    <head>
        <meta charset="UTF-8">
        <title>SportTracker - {{ gettext('Maintenance') }}</title>
        {% import 'macros.jinja2' as macros with context %}
        {{ macros.header() }}
        <link type="text/css" rel="stylesheet" href="{{ url_for('static', filename="css/maintenanceEvents.css") }}"/>
    </head>
    <body class="bg-dark text-light">
        {{ macros.navbar('maintenance') }}
        {% import 'workout/workoutMacros.jinja2' as workoutMacros with context %}
        {% import 'maintenance/maintenanceMacros.jinja2' as maintenanceMacros with context %}

        <div id="main" class="center-align">
            <section class="py-5 text-center container">
                <div class="row mb-3">
                    <div class="col col-12 mx-auto">
                        {% call macros.headline(gettext('Maintenance')) %}
                            <i class="fa-solid fa-wrench fa-fw fs-2"></i>
                        {% endcall %}
                    </div>
                </div>

                <div class="row py-3">
                    {% set eventLimit = 4 %}

                    <div class="col col-12 col-md-8 col-lg-5 mx-auto mb-5">
                        <div class="card h-100">
                            <div class="card-header mb-0">
                                <div class="d-flex align-items-center justify-content-between">
                                    <div class="d-flex align-items-center text-truncate">
                                        {{ workoutMacros.workout_type_icon_small(maintenanceWithEvents.type, fontSize='fs-3') }}
                                        <div class="fs-5 text-truncate planned-tour-name ps-3 pe-2">{{ maintenanceWithEvents.description | e }}</div>
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <a href="{{ url_for('maintenances.edit', maintenance_id=maintenanceWithEvents.id) }}" class="link-light text-decoration-none d-inline-flex align-items-center button-edit-maintenance me-2">
                                            <span class="rounded-circle bg-primary p-1 fs-5 material-symbols-outlined filled">edit</span>
                                        </a>
                                        <a href="{{ url_for('maintenanceEventInstances.add', maintenance_id=maintenanceWithEvents.id) }}" class="link-light text-decoration-none d-inline-flex align-items-center button-new-maintenance-event">
                                            <span class="rounded-circle bg-primary p-1 fs-5 material-symbols-outlined filled">add</span>
                                        </a>
                                    </div>
                                </div>
                            </div>

                            <div class="card-body d-flex flex-column ps-5 pb-4 text-start">
                                {% for maintenanceEvent in maintenanceWithEvents.events | reverse %}
                                    {% if loop.index == 4 and maintenanceWithEvents.events | length > eventLimit %}
                                        <button class="btn btn-primary align-self-center d-flex align-items-center toggle-button-hide" type="button" data-bs-toggle="collapse" data-bs-target="#maintenance-collapse">
                                            <span class="material-symbols-outlined fs-5">keyboard_arrow_down</span>&nbsp;{{ gettext('Show all (+%(remaining)d)', remaining=maintenanceWithEvents.events|length - loop.index + 1) }}
                                        </button>

                                        <div class="collapse" id="maintenance-collapse">
                                        <div class="d-flex flex-column">
                                    {% endif %}

                                    <ul class="timeline-with-icons {% if maintenanceEvent.id is none %}timeline-dashed{% endif %}">
                                        <li class="timeline-item">
                                            {{ maintenanceMacros.render_maintenance_event(maintenanceWithEvents, maintenanceEvent) }}

                                            {% if not loop.last %}
                                                <div class="text-start fst-italic">
                                                    <span class="text-secondary">{{ maintenanceEvent.numberOfDaysSinceEvent }} {{ gettext('days') }}</span>
                                                </div>

                                                    <div class="mt-3 mb-4 text-start">
                                                        {% if maintenanceWithEvents.type.name != 'FITNESS' %}
                                                            {{ (maintenanceEvent.distanceSinceEvent // 1000) }} km
                                                        {% endif %}
                                                    </div>
                                            {% endif %}
                                        </li>
                                    </ul>
                                    {% if loop.last and maintenanceWithEvents.events | length > eventLimit and loop.index >= eventLimit %}
                                        </div>
                                        </div>
                                    {% endif %}
                                {% endfor %}
                            </div>

                            {% if maintenanceWithEvents.custom_workout_field_name is not none and maintenanceWithEvents.custom_workout_field_value is not none %}
                                <div class="card-footer text-body-secondary d-flex align-items-center justify-content-center">
                                    <span class="material-symbols-outlined me-1 fs-5">filter_alt</span>
                                    <span class="small">"{{ maintenanceWithEvents.custom_workout_field_name | e }}" == "{{ maintenanceWithEvents.custom_workout_field_value | e }}"</span>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </section>
        </div>

        {{ macros.scripts() }}
        <script defer src="{{ url_for('static', filename='js/maintenancesCommon.js') }}" type="text/javascript"></script>
        <script defer src="{{ url_for('static', filename='js/maintenances.js') }}" type="text/javascript"></script>
    </body>
</html>
